# Frontend environment — copy to frontend/.env.local (optional)
# The unified dev server serves the API at /api on the same port (3000).
# Only set NEXT_PUBLIC_API_URL if you run the legacy split backend on :3001.

# NEXT_PUBLIC_API_URL=/api
