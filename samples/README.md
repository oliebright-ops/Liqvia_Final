# Sample CSV Files

Valid sample files for MVP upload templates. Each file passes `validateUpload()` in `@liqvia2/shared`.

Download via API: `GET /api/uploads/templates/{type}/sample`

| File                         | Template type       |
| ---------------------------- | ------------------- |
| trial-balance-sample.csv     | `trial_balance`     |
| ar-ageing-sample.csv         | `ar_ageing`         |
| ap-ageing-sample.csv         | `ap_ageing`         |
| bank-balances-sample.csv     | `bank_balances`     |
| bank-transactions-sample.csv | `bank_transactions` |
| budget-sample.csv            | `budget`            |

Demo company datasets (Step 17): `demo-data/`
